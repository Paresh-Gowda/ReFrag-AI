# Database Connection Configuration
# For PostgreSQL: postgresql://username:password@localhost:5432/refrag_db
# If left empty or unreachable, system automatically uses SQLite fallback
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/refrag_ai

# Upload Configuration
MAX_UPLOAD_SIZE_MB=500
